{
	"$schema": "../../../../schema/component-manifest.schema.json",
	"name": "Footer",
	"type": "content-element",
	"version": "1.0.1",
	"min_core_version": "1.0.0",
	"description": "Site footer with brand (logo text or image), nested link columns, contacts, social links, legal links, and copyright. Layouts: 4-column light, compact single row, dark split with a claim + CTAs, or maximal dark with a giant wordmark.",
	"when_to_use": "Exactly one Footer per site, placed on the shared layout. Pick split or maximal for brands that want a bold dark closing; compact for minimal sites.",
	"capabilities": [
		"logo as text or uploaded image (svg/png/webp)",
		"nested link columns (cols[] with links[] inside)",
		"claim / call-out line with primary + secondary CTA (split variant)",
		"giant wordmark treatment with contact links (maximal variant)",
		"social, legal, and copyright rows"
	],
	"variants": [
		{
			"value": "columns",
			"description": "Light 4-column layout: brand + tagline left, link columns right. Default."
		},
		{
			"value": "compact",
			"description": "Single-row footer: brand, few links, legal."
		},
		{
			"value": "split",
			"description": "Dark, claim + cta_primary/cta_secondary left, link columns right."
		},
		{
			"value": "maximal",
			"description": "Dark with a giant wordmark, contacts[] links, and legal row."
		}
	],
	"atoms": [
		"Button"
	],
	"breakpoints": {
		"mobile": "Columns stack vertically; wordmark scales down.",
		"desktop": "Multi-column grid; legal row spans the bottom."
	},
	"figma": {
		"file": "Factory_Library_WORKING_260723",
		"fileKey": "avP1L4LoOxY5ukjT4bAajX",
		"nodeId": "773:42570",
		"dateStamp": "2026-07-23",
		"moduleHeader": null
	},
	"composer_dependencies": [],
	"npm_dependencies": [
		"@iconify/vue"
	],
	"ai": {
		"prompt_summary": "Footer: site footer. Settings: variant (columns|compact|split|maximal), logo_text, logo_image, tagline (columns), claim (split/maximal), address (one line per row), cta_primary + cta_secondary (split only), copyright. Collections: cols[] { title, links[] { label, to } }, contacts[] { label, to } (maximal, e.g. tel:/mailto:), socials[] { label (e.g. IN, LI, YT), to }, legal[] { label, to }.",
		"content_guidelines": "2-4 link columns with 3-6 links each. Claim one strong sentence. Legal links: Impressum, Datenschutz, AGB. Social labels are short abbreviations, not icon names.",
		"field_hints": {
			"address": "Multiline — one address line per row.",
			"contacts": "Only rendered by the maximal variant; use tel:/mailto: links.",
			"claim": "Used by split and maximal variants as the large statement."
		}
	}
}
